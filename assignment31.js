//QUESTION: 31
let hello_admin = ["rimsha", "admin", "adil", "tayyaba", "shaban", "adeel"];
if (hello_admin.length === 0) {
    console.log("we need to finf some users.");
}
else {
    hello_admin = [];
    console.log(`all users have been removed ${hello_admin.length}`);
}
export {};
